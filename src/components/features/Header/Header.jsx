import "./Header.css";
import React from "react";
import { Button } from "@mui/material";


function Header() {
  return (
    <div className="header">
      <Button>test</Button>
      <Button>home</Button>
      <Button>shop</Button>
      <Button>about</Button>

  
    </div>
  );
};

export default Header;
